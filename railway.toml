# Railway config for Pidaka
# https://docs.railway.com/config-as-code

[build]
builder = "railpack"
buildCommand = "npm install --include=dev && npm run build"

[deploy]
startCommand = "npm run db:push && npm start"
healthcheckPath = "/"
restartPolicyType = "on_failure"
